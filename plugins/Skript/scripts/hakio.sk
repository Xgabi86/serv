command /playerinfo:
    trigger:
        set {money1} to player's balance 
        set {username} to player's name
        send "&eNom : %{username}%"
        send "&eNom : %{money1}%" 

 # ----------------------------------------------------------------

on command "sk reload":
    broadcast "un skript a été reload"

 # ----------------------------------------------------------------

command /opfree [<player>]:
    trigger:
        op the player
        send "%arg-2% tu es op mageule"

    

